import styled from 'styled-components';
export const LoginPage = styled.div`
	height: 90vh;
	.login-card {
		width: 300px;
		margin: 0 auto;
		position: relative;
		top: 50%;
		transform: translateY(-60%);
	}
	
`;
